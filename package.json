{
  "name": "simdrive",
  "version": "1.0.0",
  "description": "Drive real OpenStreetMap streets in 3D, in the browser.",
  "private": true,
  "scripts": {
    "build:css": "sass scss/main.scss css/main.css --no-source-map --style=expanded",
    "watch:css": "sass --watch scss/main.scss css/main.css --no-source-map"
  },
  "devDependencies": {
    "sass": "^1.77.0"
  }
}
